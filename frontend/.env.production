# Production build: the Mail backend is proxied by nginx at /mail-api/ ->
# 127.0.0.1:8000/api/ (see /etc/nginx/conf.d/chatita.ai.conf). The bare /api
# path on chatita.ai is a DIFFERENT, auth-protected service (returns 401), so
# the frontend MUST target /mail-api here.
VITE_API_BASE=/mail-api
